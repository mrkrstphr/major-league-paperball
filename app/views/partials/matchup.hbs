{{#unless betweenInnings}}
<div>
  <div class="mb-3">
    <div class="text-lg">Pitching</div>
    <div class="font-bold text-lg">{{matchup.pitcher.name}}</div>
    <div class="text-base">{{matchup.pitcher.line}}</div>
  </div>

  <div>
    <div class="text-lg">Batting</div>
    <div class="font-bold text-lg">{{matchup.batter.name}}</div>
    <div class="text-base">{{matchup.batter.line}}</div>
  </div>
</div>
{{/unless}}
